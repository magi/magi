export interface LabelListItem {
    id: number;
    name: string;
    code: string;
}

export interface LabelCreateInfo {
    name: string;
    code: string;
}

export interface LabelUpdateInfo {
    name: string;
}

